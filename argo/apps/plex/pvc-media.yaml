apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: plex-media
  namespace: media
spec:
  accessModes: [ReadWriteMany]
  storageClassName: nfs-media
  resources:
    requests:
      storage: 8Ti
